{
  "name": "mta-vscode-extension-builder",
  "version": "8.0.0",
  "private": true,
  "description": "Build orchestrator for MTA VSCode extensions from konveyor/editor-extensions",
  "type": "module",
  "scripts": {
    "pull-upstream": "node scripts/pull-upstream.js",
    "update-upstream": "./scripts/update-upstream.sh",
    "clean": "rm -rf .upstream-workspace"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/migtools/editor-extensions.git"
  },
  "license": "Apache-2.0",
  "dependencies": {
    "yaml": "^2.7.0"
  }
}
